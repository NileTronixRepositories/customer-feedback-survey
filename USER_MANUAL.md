# دليل استخدام نظام Customer Feedback Survey

## 1. نبذة عن النظام

هذا النظام هو واجهة Angular لإدارة استبيانات رضا العملاء داخل بيئة متعددة الفروع والإدارات. النظام يعمل بصلاحيات مختلفة حسب نوع المستخدم، ويغطي:

- إدارة الفروع والإدارات والمناطق المسؤولة عن الفروع.
- إدارة مسؤولي الفروع ومسؤولي الإدارات ومستخدمي الفروع والمشغلين.
- إنشاء وإدارة الأسئلة ومجموعات الأسئلة.
- إنشاء قوالب استبيانات داخلية مخصصة للمشغلين.
- إنشاء قوالب استبيانات عامة Anonymous لها رابط عام و QR Code.
- استقبال الردود من المشغلين أو من رابط الاستبيان العام.
- عرض تقارير ولوحات متابعة حسب الفرع أو القسم أو النظام بالكامل.

النظام يعتمد على Backend API مضبوط حاليًا على:

```text
http://192.168.1.9:8011
```

القيمة موجودة في:

- `src/environments/environment.ts`
- `src/environments/environment.development.ts`
- `public/app-config.json`

ملف `app-config.json` يتم تحميله وقت تشغيل التطبيق، وبالتالي يمكن تغيير رابط الـ API في النسخة المنشورة بدون إعادة بناء الكود.

## 2. تشغيل النظام محليًا

### المتطلبات

- Node.js متوافق مع Angular 21.
- npm، والمشروع معرف على `npm@11.12.1`.
- Backend API يعمل ومتاح من الجهاز أو الشبكة.

### خطوات التشغيل

1. افتح Terminal داخل مجلد المشروع:

```text
C:\Users\win10\OneDrive\Desktop\cutomer-feedback-survey
```

2. ثبت الاعتمادات إذا لم تكن مثبتة:

```bash
npm install
```

3. شغل التطبيق:

```bash
npm start
```

أو:

```bash
ng serve
```

4. افتح المتصفح على:

```text
http://localhost:4200/
```

لو تم فتح `/` مباشرة، النظام يوجه المستخدم حسب حالته:

- غير مسجل دخول: إلى `/auth/login`
- مسجل دخول: إلى الشاشة المناسبة حسب الدور والصلاحيات
- يحتاج تغيير كلمة مرور: إلى `/auth/change-password`

## 3. تشغيل نسخة Production

لإنشاء نسخة جاهزة للنشر:

```bash
npm run build
```

المخرجات تكون تحت:

```text
dist/customer-feedback-survey/browser
```

عند النشر على IIS أو Nginx أو Apache:

- انشر محتويات مجلد `browser`.
- تأكد أن السيرفر يعيد أي Route داخل التطبيق إلى `index.html`.
- عدل `app-config.json` داخل النسخة المنشورة إذا تغير رابط الـ API.

مثال `app-config.json`:

```json
{
  "apiBaseUrl": "http://192.168.1.9:8011"
}
```

## 4. تسجيل الدخول والجلسة

مسار تسجيل الدخول:

```text
/auth/login
```

المستخدم يدخل:

- اسم المستخدم أو البريد الإلكتروني.
- كلمة المرور.

بعد تسجيل الدخول يحدث أحد السيناريوهات:

- دخول مباشر إلى الشاشة المناسبة حسب الدور.
- اختيار فرع إذا كان الحساب مرتبطًا بأكثر من فرع، مثل نوع `BranchArea`.
- إجبار المستخدم على تغيير كلمة المرور إذا كان أول دخول أو كلمة المرور منتهية.

مسار تغيير كلمة المرور:

```text
/auth/change-password
```

كلمة المرور الجديدة يجب أن:

- تكون موجودة.
- لا تقل عن 8 أحرف.
- تطابق حقل تأكيد كلمة المرور.

الجلسة تحفظ في `localStorage` باسم داخلي مثل `cfs_session` و `cfs_token`، وتنتهي تلقائيًا بعد حوالي ساعتين. عند انتهاء الجلسة يتم توجيه المستخدم إلى صفحة الدخول.

## 5. عناصر الواجهة العامة

بعد تسجيل الدخول تظهر واجهة رئيسية تحتوي على:

- Sidebar فيه القوائم المتاحة حسب الدور والصلاحيات.
- Header يعرض نوع المستخدم أو دوره والفرع الحالي إن وجد.
- زر تغيير اللغة بين العربية والإنجليزية.
- زر تغيير كلمة المرور.
- زر تسجيل الخروج.
- رسائل نجاح أو خطأ تظهر كـ Toast عند عمليات الحفظ أو الفشل.

القوائم لا تظهر لكل المستخدمين بنفس الشكل. ظهور كل شاشة يعتمد على الدور والصلاحيات القادمة من الـ API.

## 6. الأدوار الأساسية

### Super Admin

مسؤول النظام الرئيسي. يستطيع، حسب الصلاحيات، إدارة:

- الفروع.
- مناطق الفروع Branch Areas.
- الإدارات.
- قوالب النظام.
- الأسئلة العامة ومجموعاتها.
- القوالب العامة Anonymous Templates.
- التقارير العامة وتقارير الفروع.
- إنشاء Super Admin آخر إذا كانت الصلاحية متاحة.
- إعادة تعيين كلمات مرور بعض المستخدمين.

### Branch Admin

مسؤول الفرع. يعمل داخل نطاق فرع معين، ويستطيع حسب الصلاحيات:

- عرض لوحة الفرع.
- إدارة قوالب الفرع الداخلية.
- إدارة الأسئلة ومجموعات الأسئلة.
- إدارة مستخدمي الفرع وأدوارهم.
- إدارة القوالب العامة الخاصة بالفرع.
- عرض تقارير الفرع وتنزيل تقارير PDF أو Excel.

### Branch Area

مستخدم يمثل منطقة مسؤولة عن أكثر من فرع. بعد تسجيل الدخول يختار الفرع المطلوب، ثم يعمل داخل نطاق الفرع المختار.

### Department Admin

مسؤول قسم. يستطيع حسب الصلاحيات:

- إدارة المشغلين Operators.
- ربط المشغلين بالقوالب.
- عرض لوحة تقارير القسم.
- متابعة ردود المشغلين وتفاصيل كل رد.

### Operator

المستخدم الذي يملأ الاستبيانات الداخلية المعينة له. يدخل إلى:

```text
/operator/templates
```

ويستطيع:

- رؤية القوالب المعينة له.
- اختيار قالب.
- إدخال بيانات إضافية إن وجدت.
- الإجابة على الأسئلة.
- رفع صوت أو صورة إذا كان نوع السؤال يتطلب ذلك.
- إرسال الرد.

### Public Anonymous Respondent

مستخدم خارجي لا يحتاج تسجيل دخول. يدخل من رابط عام أو QR Code:

```text
/survey/:anonymousTemplateId
```

ثم يملأ بيانات المستجيب والأسئلة الظاهرة، وبعد الإرسال ينتقل إلى صفحة نجاح.

## 7. دليل مسؤول النظام Super Admin

### 7.1 الفروع Branches

المسار:

```text
/branches
```

الاستخدام:

- عرض قائمة الفروع.
- البحث والفلترة والتنقل بين الصفحات.
- إنشاء فرع جديد.
- تعديل بيانات فرع.
- حذف فرع بعد تأكيد.
- فتح تفاصيل فرع.

داخل تفاصيل الفرع يمكن إدارة:

- بيانات الفرع.
- مسؤولي الفرع.
- الإدارات المرتبطة بالفرع.
- مسؤولي الإدارات.
- مستخدمي الفرع.
- القوالب والأسئلة المرتبطة بالفرع.
- إعادة تعيين كلمات المرور للمستخدمين المسموح بإدارتهم.

### 7.2 مناطق الفروع Branch Areas

المسارات:

```text
/branch-areas
/branch-areas/create
/branch-areas/:branchAreaId
```

الاستخدام:

- عرض مناطق الفروع.
- إنشاء منطقة جديدة باسم وبيانات دخول.
- اختيار الفروع التابعة للمنطقة.
- تعديل بيانات المنطقة.
- تعيين أو إزالة فروع من المنطقة.
- تعطيل أو استرجاع المنطقة حسب الصلاحيات.

### 7.3 الإدارات Departments

المسارات:

```text
/departments
/departments/:departmentId
```

الاستخدام:

- عرض الإدارات.
- إنشاء إدارة.
- تعديل أو حذف أو استرجاع إدارة.
- فتح تفاصيل إدارة.
- إنشاء Department Admin داخل إدارة.
- تعطيل أو استرجاع Department Admin.
- إعادة تعيين كلمة مرور Department Admin عند السماح.

### 7.4 كتالوج القوالب Templates

المسار:

```text
/templates
```

الاستخدام:

- عرض القوالب الداخلية Authorized والقوالب العامة Anonymous على مستوى كل الفروع.
- البحث حسب الاسم أو الوصف.
- الفلترة حسب الفرع والنوع والحالة.
- نسخ Template ID أو Public URL.
- نسخ قالب إلى فرع آخر عند توفر الصلاحية.

### 7.5 مجموعات الأسئلة العامة

المسار:

```text
/global-question-groups
```

الاستخدام:

- عرض مجموعات الأسئلة العامة.
- إنشاء مجموعة.
- تعديل مجموعة.
- حذف أو استرجاع مجموعة.
- التحكم في الحالة والبحث والفرز.

### 7.6 الأسئلة العامة

المسار:

```text
/global-questions
```

الاستخدام:

- إنشاء وإدارة أسئلة عامة يمكن استخدامها في القوالب العامة.
- ربط السؤال بمجموعة.
- تحديد نوع الإجابة.
- إضافة اختيارات عند الحاجة.
- حذف أو استرجاع الأسئلة.

### 7.7 التقارير العامة

المسارات:

```text
/reports/survey-dashboard
/reports/survey-dashboard/internal-responses
/reports/system-dashboard
/reports/system-responses
```

الاستخدام:

- لوحة موحدة تجمع الردود الداخلية والردود العامة.
- فلترة حسب المصدر، الفرع، القالب، التاريخ، وطريقة التجميع.
- متابعة المتوسطات، الرضا، الشكاوى، الصوتيات، الأداء حسب القالب أو الفرع.
- فتح تفاصيل الردود الحرجة.
- عرض تاريخ ردود النظام وتفاصيل كل رد.

## 8. دليل مسؤول الفرع Branch Admin

### 8.1 لوحة الفرع

المسارات:

```text
/branch-admin
/branch-admin/responses
/branch-admin/templates/dashboard
/branch-admin/templates/responses
```

الاستخدام:

- متابعة أداء الفرع.
- عرض الردود الداخلية.
- فلترة النتائج حسب التاريخ والقالب والنتيجة.
- فتح تفاصيل الرد، بما فيها الإجابات والملفات الصوتية أو الصور إن وجدت.

### 8.2 قوالب الفرع الداخلية Authorized Templates

المسارات:

```text
/branch-admin/templates
/branch-admin/templates/:templateId
/branch-admin/templates/:templateId/questions
```

الاستخدام:

- إنشاء قالب داخلي للفرع.
- تحديد الاسم والوصف وفترة التفعيل.
- إضافة Custom Inputs.
- تعديل أو حذف أو استرجاع قالب.
- ربط الأسئلة بالقالب.
- ترتيب الأسئلة.
- إدارة المنطق الشرطي بين الأسئلة.
- معاينة شكل القالب قبل استخدامه.

### 8.3 الأسئلة

المسارات:

```text
/branch-admin/questions
/branch-admin/question-groups/:groupId/questions
```

أنواع الأسئلة المدعومة:

- اختيار واحد Single Choice.
- إجابة صوتية Voice.
- تقييم نجوم Star Rating.
- شكوى أو نص Complain.
- Smiles.
- صورة Image.

الاستخدام:

- إنشاء سؤال.
- تعديل سؤال.
- حذف أو استرجاع سؤال.
- إضافة اختيارات لأسئلة الاختيار الواحد.
- البحث والفلترة حسب المجموعة والحالة.

### 8.4 مجموعات الأسئلة

المسار:

```text
/branch-admin/question-groups
```

الاستخدام:

- إنشاء مجموعة أسئلة.
- تعديل أو حذف أو استرجاع مجموعة.
- فتح أسئلة مجموعة محددة.

### 8.5 مستخدمو الفرع

المسار:

```text
/branch-admin/users
```

الاستخدام:

- عرض مستخدمي الفرع.
- إنشاء Branch User جديد.
- اختيار الأدوار المخصصة له.
- تعديل بيانات المستخدم.
- تعطيل أو استرجاع المستخدم.
- إعادة تعيين كلمة المرور.
- إعادة تعيين الأدوار.

### 8.6 تقارير القوالب PDF / Excel

المسار:

```text
/branch-admin/reports/templates-pdf
```

الاستخدام:

- اختيار قالب أو أكثر.
- معاينة التقرير.
- تنزيل التقرير PDF.
- تنزيل التقرير Excel.

## 9. دليل مسؤول القسم Department Admin

### 9.1 المشغلون Operators

المسار:

```text
/operators
```

الاستخدام:

- عرض المشغلين.
- إنشاء مشغل جديد.
- تعديل بيانات مشغل.
- تعطيل أو استرجاع مشغل.
- إعادة تعيين كلمة المرور.
- تعيين القوالب المتاحة للمشغل.

إذا كان المستخدم Department Admin عاديًا، يتم التعامل غالبًا داخل قسمه الحالي. إذا كان Super Admin يستخدم نفس الشاشة، قد تظهر له اختيارات إضافية مثل اختيار القسم.

### 9.2 تقارير القسم

المسارات:

```text
/reports/department/dashboard
/reports/department/operators/:operatorId/responses
/reports/department/operators/:operatorId/responses/:surveyResponseId
```

الاستخدام:

- عرض إجمالي ردود القسم.
- متابعة أداء المشغلين.
- متابعة أداء القوالب.
- عرض أقل الأسئلة تقييمًا.
- عرض الردود الحرجة.
- فتح ردود مشغل معين.
- فتح تفاصيل رد محدد.

## 10. دليل المشغل Operator

المسار:

```text
/operator/templates
```

خطوات الاستخدام:

1. يدخل المشغل بحسابه.
2. تظهر القوالب المعينة له.
3. يختار القالب المطلوب.
4. يملأ بيانات Custom Inputs إذا كانت مطلوبة.
5. يجيب على الأسئلة.
6. الأسئلة الشرطية تظهر أو تختفي حسب الإجابات.
7. يرفع ملف صوت أو صورة إذا كان السؤال يتطلب ذلك.
8. يراجع الملخص.
9. يرسل الرد.

بعد الإرسال يظهر ملخص الرد، ويمكن للمشغل إرسال رد آخر لنفس القالب إذا كان ذلك متاحًا.

## 11. دليل الاستبيان العام Anonymous Survey

المسار العام:

```text
/survey/:anonymousTemplateId
```

هذا المسار لا يحتاج تسجيل دخول.

خطوات المستخدم الخارجي:

1. يفتح الرابط أو يمسح QR Code.
2. ينتظر تحميل القالب.
3. يملأ بيانات المستجيب المطلوبة.
4. يجيب على الأسئلة الظاهرة.
5. الأسئلة الشرطية تظهر حسب الإجابات.
6. يرفع صورة أو صوت إذا كانت مطلوبة.
7. يضغط Submit response.
8. ينتقل إلى صفحة نجاح:

```text
/survey/:anonymousTemplateId/success
```

قد لا يفتح الاستبيان إذا:

- الرابط غير صحيح.
- القالب غير موجود.
- القالب غير مفعل.
- تاريخ بداية القالب لم يحن بعد.
- القالب منتهي.
- لا توجد أسئلة فعالة.

## 12. الصلاحيات وطريقة ظهور الشاشات

النظام لا يعتمد على الدور فقط، بل يستخدم أيضًا `permissions` و `roles` القادمة من الـ API.

أمثلة:

- `Reports.ViewBranchReports` لعرض تقارير الفرع.
- `Reports.ViewSystemDashboard` لعرض لوحة النظام.
- `Reports.ViewDepartmentReports` لعرض تقارير القسم.
- `Templates.Create`, `Templates.Update`, `Templates.Delete` لإدارة القوالب.
- `Questions.ViewAll`, `Questions.Create`, `Questions.Update`, `Questions.Delete` لإدارة الأسئلة.
- `BranchUsers.Create`, `BranchUsers.AssignRoles`, `BranchUsers.ResetPassword` لإدارة مستخدمي الفرع.
- `AnonymousTemplates.ViewAll`, `AnonymousTemplates.ViewResponses` لإدارة القوالب العامة والردود.

إذا لم يملك المستخدم الصلاحية، غالبًا:

- لن تظهر الشاشة في القائمة الجانبية.
- أو سيتم تحويله إلى `/dashboard`.
- أو تظهر رسالة عدم وجود صلاحية من الـ API.

## 13. مسارات مهمة

| الاستخدام | المسار |
| --- | --- |
| تسجيل الدخول | `/auth/login` |
| تغيير كلمة المرور | `/auth/change-password` |
| توجيه تلقائي حسب الدور | `/dashboard` |
| الفروع | `/branches` |
| مناطق الفروع | `/branch-areas` |
| الإدارات | `/departments` |
| كتالوج القوالب | `/templates` |
| القوالب العامة | `/anonymous-templates` |
| لوحة القوالب العامة | `/anonymous-templates/dashboard` |
| ردود القوالب العامة للفرع | `/anonymous-templates/responses` |
| لوحة الفرع | `/branch-admin` |
| قوالب الفرع | `/branch-admin/templates` |
| أسئلة الفرع | `/branch-admin/questions` |
| مجموعات أسئلة الفرع | `/branch-admin/question-groups` |
| مستخدمو الفرع | `/branch-admin/users` |
| تقرير PDF/Excel للقوالب | `/branch-admin/reports/templates-pdf` |
| المشغلون | `/operators` |
| مساحة عمل المشغل | `/operator/templates` |
| تقارير القسم | `/reports/department/dashboard` |
| لوحة الاستبيانات الموحدة | `/reports/survey-dashboard` |
| لوحة النظام | `/reports/system-dashboard` |
| تاريخ ردود النظام | `/reports/system-responses` |
| الاستبيان العام | `/survey/:anonymousTemplateId` |

## 14. حل مشاكل شائعة

### التطبيق لا يفتح محليًا

- تأكد من تشغيل `npm install`.
- تأكد من تشغيل `npm start`.
- افتح `http://localhost:4200/`.
- إذا كان المنفذ 4200 مشغولًا، شغل Angular على منفذ آخر باستخدام Angular CLI.

### تسجيل الدخول يفشل

- تأكد من صحة اسم المستخدم أو البريد وكلمة المرور.
- تأكد أن الـ API يعمل على الرابط الموجود في `app-config.json`.
- تأكد أن المستخدم له أدوار وصلاحيات.

### تظهر شاشة اختيار فرع

هذا طبيعي لحسابات مرتبطة بأكثر من فرع. اختر الفرع الذي تريد العمل عليه. بعد الاختيار يتم تحميل صلاحيات وبيانات هذا الفرع.

### النظام يطلب تغيير كلمة المرور

هذا يحدث عند أول دخول أو عند انتهاء صلاحية كلمة المرور. أدخل كلمة مرور جديدة ثم أعد استخدام النظام.

### تظهر رسالة Forbidden أو لا تظهر شاشة معينة

هذا يعني أن الحساب لا يملك الصلاحية المطلوبة. يجب مراجعة مسؤول النظام أو إعدادات الأدوار في الـ Backend.

### الاستبيان العام لا يفتح

تحقق من:

- صحة الرابط.
- حالة القالب.
- تاريخ بداية وانتهاء القالب.
- وجود أسئلة فعالة داخل القالب.
- اتصال المتصفح بالـ API.

### Refresh على Route داخلي في Production لا يعمل

يجب ضبط السيرفر ليسترجع `index.html` لأي Route داخلي مثل:

```text
/branches
/operator/templates
/survey/{id}
```

لأن التطبيق Single Page Application.

## 15. ملاحظات تشغيلية

- الواجهة مبنية بـ Angular standalone components.
- الحالة الداخلية تعتمد على Signals stores.
- HTTP requests تمر عبر interceptors لإضافة الـ token واللغة وإظهار Toasts.
- الاستبيانات العامة لا تحتاج token.
- كل المسارات الإدارية محمية بـ Auth Guard و Role/Permission checks.
- تغيير اللغة يحفظ في `localStorage` ويغير اتجاه الصفحة بين LTR و RTL.

## 16. ما تم الحفاظ عليه

هذا الدليل تم إنشاؤه كتوثيق فقط. لم يتم تعديل أي كود أو تغيير أي منطق داخل التطبيق.
